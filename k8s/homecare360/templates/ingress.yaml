apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homecare360
spec:
  ingressClassName: gce
  rules:
    - http:
        paths:
          - path: /auth
            pathType: Prefix
            backend:
              service:
                name: auth-http
                port:
                  number: 3001
